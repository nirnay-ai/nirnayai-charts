releases:

  - name: minio-operator
    namespace: minio-operator
    chart: minio/operator
    condition: minio.enabled
    wait: true
    waitForJobs: true

repositories:
  - name: minio
    url: https://operator.min.io/
